apiVersion: v1
kind: Service
metadata:
  name: {{ include "fineract-helm-chart.fullname" . }}
  labels:
    {{- include "fineract-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8443
      protocol: TCP
      name: https
  selector:
    {{- include "fineract-helm-chart.selectorLabels" . | nindent 4 }}
